Optional: add a short silent or ambient MP3 as `placeholder-silence.mp3` for HTMLAudioElement fallback when
`NEXT_PUBLIC_USE_BROWSER_TTS` is false and sessions have no `audioFileUrl`.

For local development, keep `NEXT_PUBLIC_USE_BROWSER_TTS=true` in `.env` (see `.env.example`).
